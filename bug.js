function divide(a, b) {
    if (b === 0) return "Error: division by zero";
    return a / b;
}

console.log(divide(10, 0));function divide(a, b) {
    if (b === 0) return "Error: division by zero";
    return a / b;
}

console.log(divide(10, 0));